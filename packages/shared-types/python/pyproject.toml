[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lightpick-types"
version = "0.1.0"
description = "Shared type definitions for LightPick (auto-generated from Zod schemas)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["lightpick_types"]
